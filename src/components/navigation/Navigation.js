import React from 'react';

const Navigation = () => {
    return(
        <nav style={{display: 'flex', justifyContent:'flex-end'}}>
            <p className='f3 pr3 dim underline-hover black pointer'>Sign Out</p>
        </nav>
    );
}

export default Navigation;